id: OT
name: Ottoman Empire
grid:
  - ["PORT-OT", "PORT-OT", "WAR", "CAR-P"]
  - ["NEU", "WAR", "DNG", "CAR-RW"]
  - ["EVT", "NEU", "WAR", "NEU"]

edges:
  # Existing routing
  - { from: [0,0], to: [0,1] }   # PORT-OT -> PORT-OT
  - { from: [0,2], to: [0,3] }   # WAR -> CAR-P
  - { from: [0,0], to: [1,0] }   # PORT-OT -> NEU
  - { from: [1,0], to: [2,0] }   # NEU -> EVT
  - { from: [2,0], to: [2,1] }   # EVT -> NEU
  - { from: [2,1], to: [2,2] }   # NEU -> WAR
  - { from: [2,2], to: [2,3] }   # WAR -> NEU (right bottom)
  - { from: [1,0], to: [1,1] }   # NEU -> WAR
  - { from: [1,1], to: [1,2] }   # WAR -> DNG

  # Added upward arrow from DNG to WAR (top)
  - { from: [1,2], to: [0,2] }

  # Right edge upward arrow NEU -> CAR-RW
  - { from: [2,3], to: [1,3] }

  # Bidirectional between PORT-OT (0,1) and WAR (0,2)
  - { from: [0,1], to: [0,2] }
  - { from: [0,2], to: [0,1] }
